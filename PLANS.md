# 医学教育多智能体 Demo ExecPlan

## 目标

基于 `family-agent-v0` 的现成 Cloudflare Think + Agents SDK + AI Elements 技术栈，
做一个可直接给司老师演示、后续可扩展为教学实验平台的医学教育 Demo。

第一版重点不是堆通用 AI 功能，而是把历史项目中的
“虚拟患者—临床导师—科研导师”和“实践—反馈—拓展—反思”真正落实成可控流程。

## 第一版范围

1. 保留现成登录、会话持久化、流式聊天、AI Elements 消息组件。
2. 新增医学教学病例目录，第一版至少内置 1 个标准病例。
3. 每个训练会话有明确阶段：
   - `history`：虚拟患者问诊；
   - `commit`：学生提交第一次诊断与鉴别；
   - `clinical_feedback`：临床导师反馈；
   - `evidence`：科研导师循证拓展；
   - `reflection`：学生提交最终修订与反思；
   - `completed`：完成。
4. 后端根据阶段决定系统角色与可用工具，避免模型自由跳阶段。
5. 记录结构化研究日志：阶段变化、学生提交、模型回复、工具调用、时间戳。
6. 教师可通过后端 RPC 导出单个训练会话的结构化 JSON，后续再补 CSV/批量导出。
7. 学生端隐藏 Family AI 的模型选择、费用、Workspace/MCP/浏览器 Demo 工具等无关功能。

## 技术策略

- Harness：继续使用 `@cloudflare/think`。
- 状态：每个病例训练仍是一个 `MyAssistant` Durable Object；训练状态与研究日志存 SQLite。
- 三角色：第一版由同一个 Think 会话根据阶段注入不同角色指令，保证最快形成可演示闭环；
  同时在代码中把角色 prompt 与阶段控制独立出来，后续可无缝升级为真正的三个 Think 子 Agent。
- UI：复用现有 AI Elements / shadcn，不重做组件库；只增加病例/阶段信息和结构化提交按钮。
- 模型：Demo 固定一个默认模型 route，不允许学生端自行切换，减少实验混杂。

## 执行步骤

1. [ ] 项目元信息与文档改为医学教育版。
2. [ ] 新增病例模型、阶段模型、角色提示词与研究日志 schema。
3. [ ] 改造 `MyAssistant`：按阶段生成 system context，并提供阶段推进 RPC / 工具。
4. [ ] 改造目录层：创建训练会话时绑定病例，列表显示病例标题与阶段。
5. [ ] 精简前端：去掉模型选择与家庭 AI 泛用入口，显示病例与阶段进度。
6. [ ] 增加“提交初步判断 / 进入循证 / 提交反思”交互。
7. [ ] 增加研究日志导出 RPC 和最小教师查看入口。
8. [ ] 补测试：阶段机、越级阻止、日志写入、病例绑定、路由。
9. [ ] 本地 build/test + 浏览器回归。
10. [ ] 修改 Worker 名称，部署独立医学 Demo，并做线上 smoke。

## 验收标准

- 学生能从病例入口完成一次完整的“问诊→初判→反馈→循证→反思”。
- 虚拟患者阶段不会主动泄露诊断答案。
- 阶段不能由 LLM 自由跳过，只能由明确 RPC/提交动作推进。
- 每个关键动作都落入结构化日志。
- 页面仍使用现有 AI Elements / shadcn 视觉体系。
- `npm test`、`npm run types`、`npm run build` 通过。

